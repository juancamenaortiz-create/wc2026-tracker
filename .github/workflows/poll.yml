# Score poller disabled — notifications removed from app
# To re-enable, uncomment the content below and redeploy

# name: WC 2026 Score Poller
# on:
#   schedule:
#     - cron: '*/5 15-23 * * *'
#     - cron: '*/5 0-8 * * *'
#   workflow_dispatch:
# jobs:
#   poll:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Trigger score poll
#         run: |
#           curl -s -L -X POST \
#             -H "x-cron-secret: ${{ secrets.CRON_SECRET }}" \
#             "${{ secrets.VERCEL_APP_URL }}/api/cron"
